(function () {
    function class_1() {
    }
    return class_1;
}());
"col-md-6 col-md-offset-3" >
    Hi;
{
    {
        currentUser.firstname;
    }
}
!/h1>
    < p > You;
're logged in with Angular 2 and Webpack!!</p>
    < h3 > All;
registered;
users: /h3>
    < ul >
     * ngFor;
"let user of users" >
    {};
{
    user.username;
}
({});
{
    user.firstname;
}
{
    {
        user.lastname;
    }
}
-(click);
"deleteUser(user.id)" > Delete < /a>
    < /li>
    < /ul>
    < p > ;
"['/login']" > Logout < /a></p >
    /div>;
